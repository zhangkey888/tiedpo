# Core training framework
torch==2.8.0
torchvision==0.23.0

# Hugging Face ecosystem
# NOTE: ALL_LAYERNORM_LAYERS was moved from transformers.trainer to
# transformers.pytorch_utils in newer versions; llava_trainer.py handles this via try/except
transformers==4.57.6
tokenizers==0.22.2
accelerate==1.12.0
datasets==3.6.0
peft==0.18.1

# DeepSpeed for ZeRO training
deepspeed==0.18.8

# Local trl (VDPOTrainer / SVCOTrainer / TieDPOTrainer live under trl/ in this repo)
# The system trl package is NOT used; install tyro which is a dependency of local trl
tyro==1.0.10

# Vision
Pillow==11.3.0
decord==0.6.0

# Misc
sentencepiece==0.2.1
protobuf==6.33.4
einops==0.8.1
httpx==0.28.1
packaging==25.0
wandb
